import { mockShops } from './shops';

export const mockRoute = {
  id: 'route_001',
  stops: [
    mockShops[3], // Drip District
    mockShops[5], // Concrete & Thread
    mockShops[2], // Harwick & Sons
    mockShops[4], // The Crypt
  ],
};
