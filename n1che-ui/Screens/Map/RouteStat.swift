import Foundation

// The stats shown on the ink route panels — raw value is the display label
enum RouteStat: String {
    case stops = "STOPS"
    case dist = "DIST"
    case time = "TIME"
    case upvotes = "UPVOTES"
}
