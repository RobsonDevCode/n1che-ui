import SwiftUI

// Placeholder — will become RootView (auth gate → Splash/Map)
struct ContentView: View {
    var body: some View {
        Text("N1CHE")
            .font(.bebas(FontSize.h1))
            .foregroundStyle(Color.inkCol)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.paper)
    }
}
