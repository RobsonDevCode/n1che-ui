import CoreGraphics

// Soft-card corner language: panels use the large radius, cards and
// curved buttons inside them echo it at half scale.
enum CornerRadius {
    static let soft: CGFloat = 13
    static let panel: CGFloat = 26
}
